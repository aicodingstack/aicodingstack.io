import { describe, expect, it } from 'vitest'

/**
 * Smoke test to ensure Vitest is wired and runnable in this repository.
 */
describe('vitest-smoke', () => {
  it('runs tests successfully', () => {
    expect(true).toBe(true)
  })
})
